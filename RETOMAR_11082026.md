> ⚠️ **SUPERADO em 14/08/2026.** O estado atual do projeto e o que falta para
> fechar estão em **`docs/ROADMAP_ENTREGA.md`**. Este arquivo aponta HEAD
> `d4261a0`; de lá para cá entraram a integração Jira, a correção da dobra e as
> validações. Mantido só pelo histórico — a credencial do Jira, que ele dava
> como perdida, foi localizada.

# RETOMAR — CVC IAM Analytics (11/08/2026)

Sessão encerrada às pressas (queda de energia + máquina instável). Este arquivo
vai junto com o código para poder retomar de outra máquina.

## Estado

- Branch `sig-fase1`, HEAD **d4261a0** (+ este arquivo), **pushado**.
- Nada preso na máquina antiga além de dados e scratch.

## Os 3 primeiros passos na máquina nova

1. `git pull` na `sig-fase1`.
2. `python -m pytest -q` — confirmar. **A suíte não chegou a terminar em 11/08**
   (a máquina caiu antes do resultado); a última medição do mesmo código, em
   10/08, foi **715 passed + 53 subtests**, zero regressão.
3. Decidir: reprocessar/reempacotar para a Bruna com os 6 ajustes (os números da
   tela mudam muito), ou retomar o **Card 25 (Jira)**.

## O que entrou no commit d4261a0 — os 6 apontamentos do 2º retorno da Bruna

| # | Ajuste | Onde |
|---|--------|------|
| A | Desligado recontratado: mesmo login = ok; login diferente = apontar. **762 → 24 pessoas** | `src/dominio/regras/regra_acesso_desligado.py` + `tests/test_desligado_recontratado.py` |
| B | Consulta: fusão de *apresentação* por CPF; a identidade principal segue `u.u` e cada acesso guarda `_orig` (tratativas intactas) | `_csUnificarPorCpf` (index.html) |
| C | Bloco "Sem mapeamento" no detalhe da Consulta, calculado na tela | `_csSemMapeamento` |
| D | Dedup dos perfis esperados pela mesma chave do casamento (não string crua), na grid e no Excel | `_perfNome` / `_perfDedup` / `_perfLista` |
| E | Coluna aditiva `motivo_status` em `validacao_acessos`, preenchida no rebaixamento por status indefinido; sai como "?" ao lado do badge | `schema.py`, `_SQL_BI`, `tipoBadge` |
| F | Coluna **Centro de Custo** na grid de Pendências (índice 11) | `_PEND_KEY`, `_GRID_COL_NOME`, colgroup/thead/colspan |

## Credencial do Jira

**NÃO está no GitHub.** Os dois commits locais que a carregavam
(`76f7e93` adicionava `Arquivos_origem/10082026/jira_cred.zip`, `57d8b6e`
removia) nunca foram enviados; foram descartados por `reset --soft` sobre
`origin/sig-fase1` e só as 3 linhas do `.gitignore` sobreviveram.
Verificado: `git log origin/sig-fase1 -- .../jira_cred.zip` volta vazio.

⚠️ O zip existia só na máquina antiga e está no `.gitignore` — **não vem no
`pull`**. Vai precisar obtê-lo/regerá-lo.

## Não commitar

- `Arquivos_origem/**` (o `iam_analytics.db`, os xlsx) — saída de execução local.
- `painel_run.log`, `vg_export.xlsx`, `vg_secoes.txt` — scratch.
- **Os 7 zips de `ENTREGA/`** (incl. `UPDATE_EXECUTAVEIS_v1.3.4.zip`,
  `TESTE_LOCAL_BRUNA_v1.3.2.zip`) — já custaram 994 MB de LFS. Ficaram só na
  máquina antiga; se precisar de algum, regerar pelo build.

## Card 25 (Jira) — terreno já mapeado

- O cliente Jira tem que morar **dentro de `src/visualizador/main.py`**: o painel
  é standalone e o spec não empacota o resto do `src/`.
- Já no repo: `scripts/jira/testar_credencial.py`,
  `scripts/jira/medir_resolucao_accountid.py`, e `docs/INTEGRACAO_JIRA_CARD25.md`
  (campos apurados na API ao vivo, padrão de texto fechado, saída do
  "Usuário Afetado" confirmada como intencional).
- Em aberto: (a) "Abrir chamado" salva a tratativa junto ou são dois passos?
  (b) precisa de um estado "aguardando chamado" além de pendente/resolvido?

## Pendências herdadas

- **Defender × `launcher_atualizador.exe`** — falso positivo que apaga o exe no
  meio do build e trunca o zip. Recomendação: tirar o atualizador do pacote LOCAL.
- **4 pontos que engolem erro** em `main.py`: `_resolucoes_db`,
  `_tratamentos_desligado_db`, `_tratamentos_transferido_db`,
  `_transferidos_depara` devolvem `{}` em qualquer exceção — tratativas sumiriam
  sem aviso. Corrigir *logando*, sem derrubar a tela.
- **Replay dos 61 dias** (~50 min) só se a Bruna cobrar o aging.

## Handoff em página (mesma informação)

https://claude.ai/code/artifact/8312a738-f2c1-4f04-bddd-c927e99a956f
