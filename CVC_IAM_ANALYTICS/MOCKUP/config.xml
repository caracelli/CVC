<?xml version="1.0" encoding="UTF-8"?>
<!--
  Servidor do mockup CVC IAM.
  - banco/caminho: relativo = resolvido na pasta do exe; absoluto/UNC = direto.
  - sistema/valor : escopo dos KPIs/usuarios (vazio = todos os sistemas).
  - quarentena/duracao_dias: data_fim = data_inicio + N dias.
  bi_divergencias e um SNAPSHOT fixo: so e (re)criada se faltar, ou rodando
  "visualizador.exe refresh" (o Processador faz isso a cada processamento).
-->
<config>
  <banco caminho="..\DADOS\BANCO\iam_analytics.db" />
  <sistema valor="SYSTUR" />
  <quarentena duracao_dias="90" />
</config>
