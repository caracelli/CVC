# Entrega POC Quarentena

Baixe o `POC_Quarentena_CVC.zip`, extraia numa pasta real na maquina do cliente e rode `ServidorPOC.exe`.
Instrucoes completas dentro do zip (LEIA-ME.md).

O exe grava `teste.db` e `registros.txt` na PROPRIA pasta dele — funciona em qualquer local.
